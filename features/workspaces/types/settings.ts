export interface WorkspaceColor {
    label: string;
    bg: string;
    ring: string;
    hex: string;
}

export interface WorkspaceSettingsForm {
    name: string;
    description: string;
    color: string;
}